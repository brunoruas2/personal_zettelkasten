# URL da API Go — usada apenas pelo servidor Next.js para o proxy de rewrites.
# Nunca exposta ao browser (sem prefixo NEXT_PUBLIC_).
# Em produção: não precisa setar se o Nginx roteia /api/ → porta 3001 no mesmo host.
API_URL=http://localhost:3001
